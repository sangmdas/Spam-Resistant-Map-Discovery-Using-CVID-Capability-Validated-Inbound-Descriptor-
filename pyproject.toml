[project]
name = "map-discovery-cvid-finality-reference"
version = "1.0.0"
description = "Reference implementation of map-discovery CVID, preview authority, future-contact authority, and communication finality"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"

