# Distribution manifest

This archive contains the complete source implementation, 125-test suite, example, benchmark, five deployment variations, documentation and GitHub Actions test workflow. Generated caches, bytecode, local virtual environments and benchmark output are excluded from the ZIP and TAR.GZ distributions.

The source package uses only the Python standard library. Archive integrity and a fresh test run are verified before release.
